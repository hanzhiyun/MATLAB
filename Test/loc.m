function [x] = loc(P, d)
% 通过观测点位置矩阵P和距离被测量点的距离来确定被测量点坐标
%此处的P和d均为自己尝试设定的示例
P = {[1, 5], [5, 5], [5, 1]};
d = [4, 5.6, 4];


[~, n] = size(P);
for i = 1:n-1
    H(i,:) = P{i+1} - P{1};
    b(i,1) = (norm(P{i+1})^2 - d(i+1)^2 - (norm(P{1})^2 - d(1)^2))/2;
end
x = (H'*H)\H'*b;
